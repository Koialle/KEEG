{# src/KEEG/WebsiteBundle/Resources/views/Article/form.html.twig #}

<div class="well">
  {{ form_start(form, {'attr': { 'class': 'form-horizontal'}}) }}

	{# Les erreurs générales du formulaire #}
  {{ form_errors(form) }}

  <div class="form-group">
  	
  	{# Génération du label. #}
  	{{ form_label(form.titre, "Titre de l'article", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

	{# Affichage des erreurs pour ce champ précis #}
  	{{ form_errors(form.titre) }}

	<div class="col-sm-10">
		
		{# Génération de l'input #}
		{{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
	</div>

  </div>

  <div class="form-group">
  	
  	{# Génération du label. #}
  	{{ form_label(form.auteur, "Auteur de l'article", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

	{# Affichage des erreurs pour ce champ précis #}
  	{{ form_errors(form.auteur) }}

	<div class="col-sm-10">
		
		{# Génération de l'input #}
		{{ form_widget(form.auteur, {'attr': {'class': 'form-control'}}) }}
	</div>

  </div>

  <div class="form-group">
  	
  	{# Génération du label. #}
  	{{ form_label(form.resume, "Résumé de l'article", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

	{# Affichage des erreurs pour ce champ précis #}
  	{{ form_errors(form.resume) }}

	<div class="col-sm-10">
		
		{# Génération de l'input #}
		{{ form_widget(form.resume, {'attr': {'class': 'form-control'}}) }}
	</div>

  </div>

  <div class="form-group">
  	
  	{# Génération du label. #}
  	{{ form_label(form.contenu, "Contenu de l'article", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

	{# Affichage des erreurs pour ce champ précis #}
  	{{ form_errors(form.contenu) }}

	<div class="col-sm-10">
		
		{# Génération de l'input #}
		{{ form_widget(form.contenu, {'attr': {'class': 'form-control'}}) }}
	</div>

  </div>

  <div class="form-group">

  	<hr />
  	
  	{# Génération du label. #}
  	{{ form_label(form.image, "Image", {'label_attr': {'class': 'col-sm-12 text-center text-uppercase'}}) }}

  </div>

  <div class="form-group">
  	
  	{# Génération du label. #}
  	{{ form_label(form.image.url, "Adresse de l'image", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

	{# Affichage des erreurs pour ce champ précis #}
  	{{ form_errors(form.image.url) }}

	<div class="col-sm-10">
		
		{# Génération de l'input #}
		{{ form_widget(form.image.url, {'attr': {'class': 'form-control'}}) }}
	</div>

  </div>

  <div class="form-group">
  	
  	{# Génération du label. #}
  	{{ form_label(form.image.alt, "Nom de l'image", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

	{# Affichage des erreurs pour ce champ précis #}
  	{{ form_errors(form.image.alt) }}

	<div class="col-sm-10">
		
		{# Génération de l'input #}
		{{ form_widget(form.image.alt, {'attr': {'class': 'form-control'}}) }}
	</div>

  </div>

  {{ form_widget(form.enregistrer, {'attr': {'class': 'btn btn-primary'}}) }}

  {{ form_rest(form) }}
  {{ form_end(form) }}
</div>