{# src/KEEG/ActivityBundle/Resources/views/Projects/add.html.twig #}

{% extends "KEEGWebsiteBundle::admin.html.twig" %}

{% block title %}
	Supprimer un projet {{ parent() }}
{% endblock %}


{% block body %}


	<center>
		
		<h3>Supprimer un projet</h3>

		<p>
	    	Etes-vous certain de vouloir supprimer le projet "{{ project.titre }}" ?
	  	</p>

	  	{# On met l'id du projet dans la route de l'action du formulaire #}
	  	<form action="{{ path('keeg_admin_projects_delete', {'id': project.id}) }}" method="post">
	    	<a href="{{ path('keeg_admin_projects_view', {'id': project.id}) }}" class="btn btn-default">
	      		<i class="glyphicon glyphicon-chevron-left"></i>
	      		Revenir au projet
	    	</a>
	    	{# Ici j'ai écrit le bouton de soumission à la main #}
	    	<input type="submit" value="Supprimer" class="btn btn-danger" />
	    	{# Ceci va générer le champ CSRF #}
	    	{{ form_rest(form) }}
	  	</form>

	</center>

{% endblock %}