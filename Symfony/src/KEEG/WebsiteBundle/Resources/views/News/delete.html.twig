{# src/KEEG/ActivityBundle/Resources/views/Projects/add.html.twig #}

{% extends "KEEGWebsiteBundle::admin.html.twig" %}

{% block title %}
	Supprimer une actualité {{ parent() }}
{% endblock %}


{% block body %}


	<center>
		
		<h3>Supprimer une actualité</h3>

		<p>
	    	Etes-vous certain de vouloir supprimer l'actualité "{{ news.titre }}" ?
	  	</p>

	  	{# On met l'id du news dans la route de l'action du formulaire #}
	  	<form action="{{ path('keeg_admin_news_delete', {'id': news.id}) }}" method="post">
	    	<a href="{{ path('keeg_admin_news_view', {'id': news.id}) }}" class="btn btn-default">
	      		<i class="glyphicon glyphicon-chevron-left"></i>
	      		Revenir à l'actualité
	    	</a>
	    	{# Ici j'ai écrit le bouton de soumission à la main #}
	    	<input type="submit" value="Supprimer" class="btn btn-danger" />
	    	{# Ceci va générer le champ CSRF #}
	    	{{ form_rest(form) }}
	  	</form>

	</center>

{% endblock %}