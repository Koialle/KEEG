{# src/OC/PlatformBundle/Resources/view/Advert/view.html.twig #}

{% extends "KEEGActivityBundle::layout.html.twig" %}

{% block title %}
	{{ project.titre }} - {{ parent() }}
{% endblock %}

{% block keegactivities_body %}

	{# On vérifie qu'une image est bien associée à l'annonce 
	{% if article.image is not null %}
		<img src="{{ article.image.url }}" alt="{{ article.image.alt }}" />
	{% endif %}
	#}

	<h2>{{ project.titre }}</h2>
	<i>Par {{ project.auteur }}</i>

	<center>
		<a href="{{project.url}}" title="Cliquez pour voir le projet" target="_blank">
			<img src="{{project.image}}" alt="{{project.titre}}" width="500px" height="300px">
		</a>
	</center>
	
	<div class="well">
		{{ project.contenu }}
	</div>


	<p class="text-right">
		<a href="{{ path('keeg_activity_projects') }}" class="btn btn-default">
			<i class="glyphicon glyphicon-chevron-left"></i>
				Revenir aux projets
		</a>

		{#
		<a href="{{ path('keeg_forum_edit', {'id': article.id}) }}" class="btn btn-default">
			<i class="glyphicon glyphicon-edit"></i>
		    Modifier l'annonce
		</a>
		<a href="{{ path('keeg_forum_delete', {'id': article.id}) }}" class="btn btn-danger">
		    <i class="glyphicon glyphicon-trash"></i>
		    Supprimer l'annonce
		</a>

		#}
	</p>
	
{% endblock %}